<html>
  <head>
    <meta http-equiv="X-XRDS-Location" content="<%= absolute_url(:user_xrds, {:email => @user.email}) %>" />
    <link rel="openid.server" href="<%= absolute_url(:servers) %>" />
  </head>
  <body>
    <p>OpenID identity page for <%= @user.email %></p>
  </body>
</html>
