<html>
  <head>
    <meta http-equiv="X-XRDS-Location" content="<%= absolute_url(:user, {:id => @user.login, :format => :xrds}) %>" />
    <link rel="openid.server" href="<%= absolute_url(:servers) %>" />
  </head>
  <body>
    <p>OpenID identity page for <%= @user.login %></p>
  </body>
</html>
